/**
 * Define HTML elements v-canvas, v-node, v-edge, etc.
 *
 * @example
 *
 * ```ts
 * import { defineFlowViewElements } from 'flow-view';
 *
 * addEventListener('load', () => {
 *   defineFlowViewElements();
 * });
 * ```
 */
export declare const defineFlowViewElements: () => void;
