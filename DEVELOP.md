# flow-view development

## Requirements

With [https://www.npmjs.com/] install the following packages globally:

```bash
npm install esbuild serve -g
```

## scripts

- Start demo: `npm start`
- Generate build: `npm run build`
